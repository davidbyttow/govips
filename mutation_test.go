package gimage

import "testing"

func TestMutation(t *testing.T) {
	// buf, err := ReadFile("fixtures/canyon.jpg")
	// if err != nil {
	// 	t.Fail()
	// }
	// op, err := NewMutation(buf)
	// if err != nil {
	// 	t.Fail()
	// }
	// op.Resize(800, 600, nil).Apply()
}
